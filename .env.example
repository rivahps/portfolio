# Example environment variables for local development
# Copy this file to `.env` and replace the placeholder values with your own keys.
# Do NOT commit your real `.env` file or secrets to version control.

# Email service / client keys used by the contact form (example uses EmailJS)
REACT_APP_EMAILJS_SERVICE_ID=your_service_id_here
REACT_APP_EMAILJS_TEMPLATE_ID=your_template_id_here
REACT_APP_EMAILJS_PUBLIC_KEY=your_public_key_here

# Notes:
# - For local development: create a `.env` with the real values and restart the dev server.
# - For deployment (Netlify, Vercel, etc.): set these values in the platform's environment variables UI.
